# notebooks

The content. One folder per guide, files named `NN-slug.ipynb` matching `manifest.json`.

Committed **with outputs**, so GitHub renders them and a reader can see the result before
running anything. Re-execute a notebook after editing it: committed output that disagrees
with committed code teaches the reader something false.

Every notebook has the eight-part shape. See `CLAUDE.md`.
